import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

export const Appbar: React.FC = () => {
    return (
        <React.Fragment>
            <Header></Header>
        </React.Fragment>
    );
};
export default Appbar;
